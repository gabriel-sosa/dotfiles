# My dot files

This is collection of my personal configuration files

Ensure you have the following variables in your `~/.zshenv` file:

```
export XDG_CONFIG_HOME="$HOME/.config"
export ZDOTDIR="$XDG_CONFIG_HOME/zsh"
```
